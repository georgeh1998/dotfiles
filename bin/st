#!/bin/bash
# st - Open the current directory in SourceTree

set -e

TARGET_DIR="${1:-.}"

if [ ! -d "$TARGET_DIR/.git" ]; then
    echo "Error: $TARGET_DIR is not a git repository."
    exit 1
fi

open -a SourceTree "$TARGET_DIR"
